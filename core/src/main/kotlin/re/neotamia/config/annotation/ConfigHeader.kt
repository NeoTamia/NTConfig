package re.neotamia.config.annotation

//@Target(AnnotationTarget.CLASS)
///**
// * @param header The header to be added at the top of the configuration file. Output as a comment in the file if the format supports it.
// */
//annotation class ConfigHeader(val header: String)
